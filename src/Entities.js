import * as THREE from 'three';import {CONFIG} from './config.js';
export function mat(c){return new THREE.MeshStandardMaterial({color:c,roughness:.72,metalness:.04})}
export class Character{constructor(team,name){this.team=team;this.name=name;this.planks=0;this.velocity=new THREE.Vector3();this.stun=0;this.finished=false;this.fallen=false;this.mesh=new THREE.Group();const body=new THREE.Mesh(new THREE.CapsuleGeometry(.48,.9,5,10),mat(CONFIG.colors[team]));body.position.y=1.05;this.mesh.add(body);const head=new THREE.Mesh(new THREE.SphereGeometry(.46,12,10),mat(0xffd2a1));head.position.y=1.95;this.mesh.add(head);for(const s of[-1,1]){const arm=new THREE.Mesh(new THREE.CapsuleGeometry(.13,.62,4,7),mat(CONFIG.colors[team]));arm.position.set(s*.62,1.15,0);arm.rotation.z=s*.18;this.mesh.add(arm);const leg=new THREE.Mesh(new THREE.CapsuleGeometry(.16,.68,4,7),mat(0x183d55));leg.position.set(s*.22,.45,0);this.mesh.add(leg)}const shadow=new THREE.Mesh(new THREE.CircleGeometry(.58,16),new THREE.MeshBasicMaterial({color:0x003d52,transparent:true,opacity:.22,depthWrite:false}));shadow.rotation.x=-Math.PI/2;shadow.position.y=.02;this.mesh.add(shadow);this.homePos=new THREE.Vector3();this.bob=0}updateVisual(dt,running=true){this.bob+=dt*(running?10:4);this.mesh.position.y=this.homePos.y+Math.sin(this.bob)*.035;if(running){this.mesh.rotation.z=Math.sin(this.bob)*.035}}drop(){const n=this.planks;this.planks=0;return n}}
export function makePlank(team,skin='wood'){const c=CONFIG.plankColors[team];const g=new THREE.BoxGeometry(CONFIG.plank.w,CONFIG.plank.h,CONFIG.plank.d);const m=mat(skin==='gold'?0xffd43b:skin==='ice'?0xaeeeff:skin==='neon'?c:c);const o=new THREE.Mesh(g,m);o.userData.team=team;return o}
export function makePalm(){const g=new THREE.Group();const trunk=new THREE.Mesh(new THREE.CylinderGeometry(.18,.28,3.1,7),mat(0x9a663b));trunk.position.y=1.55;trunk.rotation.z=.12;g.add(trunk);for(let i=0;i<7;i++){const leaf=new THREE.Mesh(new THREE.ConeGeometry(.23,2.5,5),mat(0x21a84a));leaf.position.y=3.1;leaf.rotation.z=Math.PI/2.2;leaf.rotation.y=i*Math.PI*2/7;g.add(leaf)}return g}
