export class InputManager{constructor(canvas){this.keys={};this.axis=0;this.drag=false;this.lastX=0;window.addEventListener('keydown',e=>{this.keys[e.code]=true;if(['ArrowLeft','ArrowRight','Space'].includes(e.code))e.preventDefault()});window.addEventListener('keyup',e=>this.keys[e.code]=false);canvas.addEventListener('pointerdown',e=>{this.drag=true;this.lastX=e.clientX;canvas.setPointerCapture?.(e.pointerId)});canvas.addEventListener('pointermove',e=>{if(this.drag){this.axis=Math.max(-1,Math.min(1,(e.clientX-this.lastX)/70));this.lastX=e.clientX}});window.addEventListener('pointerup',()=>{this.drag=false;this.axis=0})}getSteer(){let a=0;if(this.keys.KeyA||this.keys.ArrowLeft)a-=1;if(this.keys.KeyD||this.keys.ArrowRight)a+=1;return Math.abs(a)>0?a:this.axis}}
